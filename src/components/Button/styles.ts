import { styled } from "styled-components";

export const StyledButton = styled.button`
  width: 170px;
  height: 55px;
  padding: 3px;

  border-radius: 255px;
  background: black;
  color: var(--color-primary);
  font-size: 1rem;
`;
